credentials.json
out
token.json
todoist-sync-env/
__pycache__/

# Old config files (deprecated)
task_mappings.json
sync_config.json

# Todoist-Google Tasks bidirectional sync files (keep .template tracked)
todoist-sync.conf
todoist-sync-mappings.json

# Todoist to Google Tasks one-way sync files (keep .template tracked)
todoist-to-gtasks.conf

# Google Tasks recurring tasks files (keep .template tracked)
gtasks-recurring.conf
recurring_config.json

# Google Tasks TRMNL sync files (keep .template tracked)
gtasks-trmnl.conf
gtasks-trmnl-mappings.json
